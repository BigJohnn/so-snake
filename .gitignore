__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
node_modules/
# Leading slash: `dist/` unanchored would also match nothing here today, but the
# frontend's build output is the only one, and anchoring keeps it that way.
/tools/gui/frontend/dist/

# Recorded episodes and training runs live outside version control. Anchored to
# the repo root -- an unanchored `data/` also matches `src/so_snake/data/`, the
# recording package, and silently drops it from every commit.
/data/
/outputs/
wandb/
*.mp4
!image.png

.claude/settings.local.json
.vscode/
.idea/
